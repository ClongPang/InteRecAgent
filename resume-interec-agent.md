# InteRecAgent 简历项目经历

**InteRecAgent｜跨市场比价与推荐 Agent**　个人项目 / Agent 开发　2026.07–至今

**项目描述**：面向跨市场购物中的多轮需求澄清、报价检索与候选比较，构建具备需求状态跟踪、受控工具调用、候选资格校验、事实溯源与任务恢复能力的对话 Agent；对校验规则完整的品类输出正式推荐，其余品类仅提供带验证边界的候选线索。

**技术栈/数据源**：TypeScript、pi-agent-core、Fastify、PostgreSQL、DeepSeek、BuyWhere API、Langfuse、OpenTelemetry

- **多轮状态与候选指代**：针对需求与候选集合持续演进导致的状态漂移和指代失稳，以 PostgreSQL Revision 持久化购物目标与候选集，每轮从有界快照恢复上下文，并在执行前将自然语言指代绑定为稳定候选标识；11 组离线多轮轨迹覆盖状态演进、候选操作与版本回退，已有证据可用时保持零报价调用，撤销后精确恢复目标 Revision。

- **计划校验与工具授权**：针对复合请求中的漏操作、无依据参数和越权调用，设计 `commit plan → ordered operations → publish reply` 两阶段协议，由 LLM 提交语义计划，确定性执行层结合用户原文、会话状态和工具契约完成来源校验、意图编译与外调授权；构建 6 类 30 项协议对抗用例，30/30 进入预期拒绝或受控降级，未发布越权回复。

- **候选准入与事实溯源**：针对异构报价中候选语义资格、用户约束与来源证据无法直接对齐的问题，构建 `Provider Artifact → Source Fact → EvidenceRef → Claim` 证据链，按品类能力执行确定性准入与结果分级；正式回复只能引用已验证 Claim，校验能力不足时降级为带边界说明的候选线索，避免未经证明的候选或事实进入正式推荐。

- **持久执行与故障恢复**：针对 Worker 接管和进程中断引发的旧任务覆盖、工具重复执行与回复重复提交，以数据库 Lease、Fencing Token 和 Revision 校验隔离失效执行，按幂等步骤账本复用成功结果，并原子提交状态、回复与事件；PostgreSQL/API 集成门禁 23/23 通过，覆盖过期写入拒绝、幂等重放、事务回滚及 Outbox 失败隔离。

- **多轮任务评测与可观测性**：针对单轮回复正确但完整购物任务失败、模型异常难定位的问题，构建覆盖 13 类 39 项任务、预登记 117 次真实模型运行的评测框架，冻结 Prompt、模型、回放数据和评分器版本，并将模型推理、工具调用、执行步骤与最终状态关联至同一 Trace；已完成评测器、Replay Provider、漂移门禁和 Langfuse 实验链路，支持按任务族定位状态、操作与事实失败。
