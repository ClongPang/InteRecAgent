# InteRecAgent 简历稿与调整要求规范

面向应届生投递高水平公司 **Agent 开发岗**。本文件包含两部分：可直接使用的项目简历稿，以及从多轮「编写—审核—重写」中沉淀的调整要求规范，供后续迭代对照。

---

## 一、项目简历稿

**跨境购物对话式导购 Agent（InteRecAgent）**　个人项目　2026.07–2026.08
TypeScript / PostgreSQL / Fastify / pi-agent / DeepSeek / BuyWhere
用户以自然语言表达购物目标，Agent 跨市场检索比价、维护候选并给出证据支撑的推荐。

- **输出 grounding 护栏**：导购的价格、库存、市场声明无据即误导决策；对 Agent 输出做 grounding，事实绑定已验证证据、发布前经 claim-evidence 一致性校验，证据不足则拒答。可追溯率 **【100%】**、无证据事实 **【0】**。

- **会话候选记忆与按需检索**：用户逐轮改条件、引用已展示候选，逐轮全量检索冗余，直接复用又易命中过期证据或指代错绑；候选带证据与时效存入会话记忆，按覆盖度与新鲜度按需检索、其余复用记忆，指代锁定展示时快照。外部调用降约 **【~70%】**，复用轮无过期证据外露与指代错绑。

- **规划校验与自纠**：一句话含多个购物意图，LLM 规划的多步工具调用常漏调、越权或造参；内核对计划做校验与修复——补漏、去越权与冗余、未知参数拒绝，构成工具调用护栏。纠偏率约 **【~20%】**、漏调用占比降至 **【<2%】**。

- **持久化执行与副作用一致性**：一轮的目标/候选/回复须一致落地、重试下不得重复调用付费工具；有状态 Agent 每轮为可持久化执行单元，工具副作用经幂等账本与原子发布达 exactly-once。**【20】** 组重试用例 0 重复调用、0 脏读。

- **多轮轨迹评测**：导购正确性在完整多轮流程，单轮通过不足证明；为多轮 Agent 建轨迹级评测，标注工具选择/路由、共指绑定与 groundedness，区分脚本操作与真实模型轨迹以免高估，纳入 CI。路由 **【~93%】**、共指 **【~91%】**、事实一致 **【~99%】**。

> 投递前两件事：`【】` 占位数字按既定口径实测回填；对 grounding 校验、工作记忆+共指、规划修复、exactly-once、轨迹评测五点，各准备一段「解决什么、换方案的代价」的追问应答。

---

## 二、调整要求规范（对照清单）

### 1. 定位与受众
- 应届生投**高水平公司的 Agent 开发岗**，不是通用 LLM 应用岗，也不是纯推荐/后端岗。
- 以**大厂 Agent 面试官**视角审批：每句话、每个表达都要对面试有信号意义，经得起追问。

### 2. 内容选择
- 选这个对话式推荐 Agent**真正重要、真正难**的点，不是「顺手能量化」的点。
- 每条体现：**业务难点 + Agent/后端技术取舍 + 结果**。
- **Agent 技术领域显性**：grounding/护栏、规划与工具调用校验、跨轮工作记忆与共指、持久化执行与副作用一致性、轨迹级评测。
- **不写行业常识/table-stakes**：如「用了 function calling」「基础 RAG」「插件式接入 tracing」「模型规划+代码执行」等已成共识的内容。
- **结合具体业务场景**（跨境购物导购、多市场比价、跨币种、第三方报价），不悬空。
- 可把**将来会补的按已完成写**，但必须现实、可落地、符合业务与 Agent 工程，**不随便许诺/虚构**。

### 3. 结构与表达
- 每条走 **问题 → 方案 → 结果**。
- **每条约 80 字**，精炼，**不口语、不白话、不啰嗦**。
- 专业，但术语**符合行业共识**，去掉自造复合名词和面试官难懂的词。
- **不写具体商品案例**（如某型号某容量）。
- **体现具体技术**，关键机制**点名**，且能被追问时讲清取舍。

### 4. 量化
- 每条带**可实测的量化支撑**；当前用占位符，**口径合理**。
- 数字须能回答「样本多大、怎么算」；**投递前必须真实回填**，答不出的先撤。

### 5. 风格审核（去 AI 味）——三方多轮机制
- 三方：**具备 AI 风格判别的 HR + 大公司技术专家 + 本人（材料提供者）**，多轮「编写—审核—重写」。
- 去掉：通用套话、术语堆叠、模板句式（针对/基于/通过/实现/保障）、不可验证结论、自造架构名词、把内部类名包装成行业术语。
- 底线自检：**每句话只能描述你的项目、能在面试自然复述、每个术语能对应到具体实现**。

### 6. 当前 TODO（待落地）
1. 把所有 `【】` 占位数字按既定口径实测回填。
2. 对每个点名技术（grounding/claim 校验、工作记忆+共指、规划校验修复、exactly-once 副作用、轨迹评测）准备「解决什么、替代方案代价」的说法。
