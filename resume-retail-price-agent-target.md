# RetailPriceAgent 简历目标版（内部）

> 本文件包含尚未完成的评测指标占位符。所有 `【待回填】` 必须依据 `resume-retail-price-agent-evaluation-design.md` 实测并通过证据审核后才能用于投递；当前可投递事实稿仍为 `resume-retail-price-agent.md`。

**RetailPriceAgent｜商品报价查询 Agent**　个人项目 / Agent 开发　2026.07–至今

**项目描述**：面向跨市场购物中的多轮需求澄清、报价检索与候选比较，构建具备需求状态跟踪、受控工具调用、候选资格校验、事实溯源与任务恢复能力的对话 Agent；对校验规则完整的品类输出正式推荐，其余品类仅提供带验证边界的候选线索。

**技术栈/数据源**：TypeScript、pi-agent-core、Fastify、PostgreSQL、DeepSeek、BuyWhere API、Langfuse、OpenTelemetry

- **多轮状态与候选指代**：针对需求与候选集合持续演进导致的状态漂移和指代失稳，以 PostgreSQL Revision 持久化购物目标与候选集，每轮投影有界状态快照，并在执行前将自然语言指代绑定为稳定候选标识；在 117 次真实模型多轮评测中，约束状态正确【A/B，待回填】、候选指代正确【C/D，待回填】，零报价调用 Turn 合规【E/F，待回填】。

- **计划校验与工具授权**：针对复合请求中的语义遗漏、参数缺乏依据和工具越权，设计 `commit plan → ordered operations → publish reply` 两阶段协议，由 LLM 提交语义计划，确定性执行层完成来源校验、意图编译和外调授权；必需操作执行覆盖【G/H，待回填】，6 类 30 项协议对抗用例 30/30 进入预期拒绝或受控降级。

- **候选准入与事实溯源**：针对异构报价中候选语义资格、用户约束与来源证据无法直接对齐的问题，构建 `Provider Artifact → Source Fact → EvidenceRef → Claim` 证据链，按品类能力执行确定性准入和结果分级；在满足正向产出门禁的评测中，推荐候选准入正确【P/Q，待回填】、可验证事实与原始证据一致【M/N，待回填】，无依据商品事实【K，待回填】项。

- **上下文与外调效率**：以结构化 Goal、Dialogue 和 WorkingSet 投影替代无界历史消息回灌，并在已有证据满足当前需求时复用候选状态；相较全量 Transcript 基线，单 Turn 模型输入 Token 中位数降低【待回填】、任务成功率变化【待回填】，相较每轮重新检索基线，报价 Provider 调用降低【待回填】，避免以压缩上下文或减少调用换取任务质量下降。

- **任务评测与故障恢复**：构建覆盖 13 类 39 项任务的冻结回放集，以真实模型各运行 3 次并检查状态、操作、候选与事实，端到端任务通过【X/117，待回填】、三次稳定通过【Y/39，待回填】；另完成 8 类 80 次进程级故障注入，恢复断言通过【R/80，待回填】，过期执行提交【待回填】次、重复正式回复【待回填】次。

## 投递门禁

- 所有指标必须同时回填分子、分母、百分比和评测报告位置，不能只填写百分比。
- 真实模型 Gold Set、故障注入和人工复核全部完成前，本文件不得替换当前事实稿。
- 未达到预设门槛的指标必须如实保留或删除对应主张，不能改口径掩盖失败。
