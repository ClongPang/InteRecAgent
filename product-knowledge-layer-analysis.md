# RetailPriceAgent 商品知识层业务分析

## 1. 结论

如果 RetailPriceAgent 的目标保持为“新加坡市场、已知精确型号的报价线索助手”，当前的薄商品身份能力基本足够，不需要立即建设完整商品知识平台。

如果未来恢复“通用品类推荐助手”，商品知识层则是必要基础。缺少商品知识层时，系统最多能够完成自然语言搜索编排、结果过滤和报价展示，无法稳定证明：

- 不同平台记录是否属于同一商品或同一 Variant；
- 用户的自然语言需求应映射到哪些可比较属性；
- 候选是否满足硬约束；
- 一个推荐结论是否具有充分、可追溯的事实依据；
- 排序差异来自真实商品差异，还是缺失数据、不同版本或 Provider 排名。

因此，商品知识层不是为了增加一个流行的“知识图谱”组件，而是为了给商品身份、属性、关系、来源和推荐策略提供稳定、版本化、可审计的业务语义。

## 2. 为什么通用推荐需要商品知识层

### 2.1 统一跨来源商品身份

同一个商品在不同平台可能具有完全不同的标题和标识，例如：

- `Sony WH-1000XM5`；
- `WH1000XM5/B`；
- `Sony XM5 Wireless Headphones`；
- 日版、国际版、不同颜色或套装；
- 新品、翻新和二手记录；
- 耳机本体、耳罩、保护壳和维修服务。

Provider 返回的是报价或目录记录，不是天然可靠的统一商品实体。通用推荐首先必须判断：

- 哪些记录属于同一 Product；
- 哪些属于不同 Variant 或地区版本；
- 哪些是配件、耗材、替换件或服务；
- 哪些报价可以合并或直接比较；
- 哪些差异会实质影响用户决策。

如果没有稳定的商品身份，价格比较、去重、候选记忆和推荐解释都会建立在不可靠的记录标题上。

### 2.2 将用户需求映射为品类属性

用户通常不会使用标准字段表达需求，而会说：

- “适合通勤”；
- “续航长一点”；
- “不要太重”；
- “能连接两台设备”；
- “预算 2500，降噪要好”。

系统需要把这类表达映射为对应品类的属性和约束。例如“适合通勤”可能涉及主动降噪、重量、续航、佩戴方式、多设备连接和便携性。

不同品类的核心属性完全不同：

| 品类 | 典型决策属性 |
| --- | --- |
| 耳机 | 降噪、续航、重量、佩戴形式、编码、多设备连接 |
| 手机 | SoC、内存、存储、频段、地区版本、系统支持周期 |
| 显示器 | 尺寸、分辨率、刷新率、面板、亮度、接口 |
| 空气净化器 | CADR、适用面积、滤芯、噪声、功耗 |

因此，通用推荐不能依赖一套无品类差异的通用 JSON Schema，也不能让 LLM 临时决定哪些属性重要。

### 2.3 建立可靠的比较与推荐基准

报价线索只需要说明某一 Provider 在某一时间返回了什么。推荐则必须回答“为什么 A 比 B 更适合当前用户”。这要求系统掌握：

- 规范化且可比较的属性；
- 单位和枚举的统一规则；
- 硬约束与软偏好的区别；
- 缺失值的业务语义；
- 属性来源、采集时间和冲突状态；
- 品类相关的准入和排序规则。

缺少这些基础时，系统容易出现以下问题：

- 把属性缺失解释成“不支持”；
- 把营销描述当作技术规格；
- 把不同容量、地区版本、套装或成色直接比较；
- 仅根据 Provider 返回顺序或价格生成推荐；
- 由 LLM 补齐并不存在的规格或适用性结论。

### 2.4 分离稳定商品事实与动态报价事实

商品知识与报价数据具有不同生命周期，不能混为一体：

| 数据类型 | 示例 | 典型更新频率 |
| --- | --- | --- |
| 商品知识 | 型号、重量、容量、规格、兼容关系 | 较慢 |
| Offer 事实 | 商家、价格、成色、库存记录、链接 | 较快 |
| 派生决策 | 是否满足约束、比较结果、推荐分数 | 随用户目标和证据变化 |

一次 Provider 搜索结果不能长期充当商品知识；静态商品规格也不能被解释为当前价格、库存或配送事实。

建议的数据流为：

```text
Provider 原始记录
        ↓
来源适配与原始 Observation
        ↓
商品身份解析与 Variant 消歧
        ↓
Canonical Product / Product Variant
        ↓
带来源的规范化商品属性
        ↓
当前 Offer 与报价 Observation
        ↓
硬约束过滤 → 证据充分性判断 → 排序 → 推荐解释
```

## 3. 当前项目已经具备的能力

当前报价实现已经形成一个面向精确型号任务的“薄商品身份层”：

- 品牌别名和品牌规范化；
- 型号 Unicode、大小写、空格和连字符规范化；
- 型号字母或数字变化时要求确认；
- 精确型号、品牌和限定词准入；
- 主商品、配件、维修、替换件和服务角色判断；
- 新品、翻新、二手和未知成色识别；
- 商家页 URL 规范化与报价分组；
- 原始 `QuoteObservation`、准入决策和最终 `QuoteLead` 的证据关系。

主要实现位于：

- `packages/domain/src/quote-target.ts`；
- `packages/domain/src/quote-admission.ts`；
- `packages/domain/src/quote-grouping.ts`；
- `packages/domain/src/quote-types.ts`。

这套能力适合回答“帮我查 Sony WH-1000XM5 的报价”，因为用户已经提供了商品身份，系统只需要验证返回记录是否匹配。

## 4. 当前能力与通用推荐之间的缺口

当前薄身份层不能直接支撑开放式推荐，主要缺口包括：

1. **没有持久化 Canonical Product**：稳定身份仍依赖当前会话中的型号文本。
2. **没有完整 Variant/SKU 模型**：地区、容量、颜色、套装、频段和配置差异无法统一表达。
3. **品牌与规则硬编码**：新增品牌和品类需要修改源码，缺少版本化数据治理。
4. **没有品类属性注册表**：无法定义哪些属性用于身份、硬约束、排序或仅展示。
5. **没有属性冲突模型**：多个来源提供不同规格时，没有显式冲突、选择和降级语义。
6. **没有商品关系模型**：不能稳定表达配件兼容、替代型号、系列、上下代和套装包含关系。
7. **没有知识新鲜度策略**：无法区分长期稳定规格、可能变化的信息和实时 Offer。
8. **没有推荐充分性门禁**：缺乏按品类判断“当前证据是否足以给出推荐”的统一规则。
9. **没有推荐标签和反馈闭环**：无法验证排序是否真正改善用户决策。

旧推荐实现中的商品身份和品类校验代码可以视为早期尝试，但它们主要是代码内的正则和策略集合，不是一个可独立演进、可追踪数据来源的商品知识层。

## 5. 建议的商品知识层边界

### 5.1 商品身份层

建议至少定义以下实体：

- `Brand`：品牌及多语言别名；
- `ProductFamily`：产品系列或家族；
- `CanonicalProduct`：稳定产品实体；
- `ProductVariant`：容量、颜色、地区、套装等可购买变体；
- `ProductIdentifier`：MPN、GTIN、UPC、EAN、Provider ID；
- `ProductAlias`：标题别名、历史型号和来源专属名称；
- `IdentityResolution`：原始记录到 Product/Variant 的解析结果、置信度和理由。

身份解析应遵守失败关闭原则：低置信度记录保持未解析或待人工/用户确认，不能为了提高召回率自动合并。

### 5.2 品类与属性层

建议定义版本化的属性注册表：

```ts
interface AttributeDefinition {
  key: string;
  categoryId: string;
  valueType: "NUMBER" | "BOOLEAN" | "ENUM" | "TEXT";
  unit?: string;
  identityRole: "IDENTITY" | "QUALIFIER" | "DESCRIPTIVE";
  recommendationRole:
    | "HARD_CONSTRAINT"
    | "PREFERENCE"
    | "DISPLAY_ONLY";
}
```

每个属性值还应保存：

- 原始值与规范化值；
- 单位和转换规则；
- 来源和证据引用；
- 观察或生效时间；
- 解析规则版本；
- 置信度与冲突状态。

### 5.3 商品关系层

最低限度需要表达：

- `SAME_PRODUCT`；
- `VARIANT_OF`；
- `ACCESSORY_FOR`；
- `REPLACEMENT_FOR`；
- `SUCCESSOR_OF` / `PREDECESSOR_OF`；
- `BUNDLE_CONTAINS`；
- `REGIONAL_VERSION_OF`。

关系必须有方向、来源和置信度，不能只保存为自由文本。

### 5.4 Offer 与 Observation 层

实时 Offer 继续使用当前来源证据原则，至少包含：

- 商家与商家页；
- Product/Variant 引用；
- 原币价格；
- 成色；
- Provider 更新时间和本系统观察时间；
- Provider availability 原始证据；
- 原始 artifact 和解析状态。

商品知识层不能把历史 Offer 缓存重新发布为当前报价，也不能把 Provider availability 转换为未经确认的“当前有货”。

### 5.5 推荐策略层

商品知识层提供事实输入，但不直接拥有最终推荐。推荐策略层负责：

- 用户需求到属性约束的映射；
- 硬约束过滤；
- 候选身份和属性覆盖率检查；
- 缺失数据和冲突数据处理；
- 软偏好权重；
- 价格、性能和风险的取舍；
- 推荐充分性判断；
- 带证据的推荐解释。

当证据不足时，合法结果应是“候选线索”或“需要补充信息”，而不是强制输出推荐。

## 6. 推荐的技术形态

第一阶段不建议直接引入图数据库、独立向量数据库或推荐模型服务。现有 PostgreSQL 足以承载初期知识层：

```text
product_categories
attribute_definitions
brands
product_families
canonical_products
product_variants
product_identifiers
product_aliases
product_relationships
product_attribute_facts
identity_resolutions
offer_observations
```

图结构可以先通过带类型的关系表表达；语义召回可以在身份和属性质量稳定后，再评估 PostgreSQL `pgvector` 或外部检索服务。

优先级应该是：

1. 身份准确；
2. 属性有来源；
3. Variant 不被错误合并；
4. 硬约束绝不越过；
5. 推荐解释能够回到事实；
6. 最后再优化召回和排序。

## 7. 分阶段实施建议

### 阶段 A：身份与品类基础

- 选择 2–3 个边界清晰的品类；
- 建立 Canonical Product、Variant 和 Identifier；
- 把品牌、别名和型号规则从业务代码迁移为版本化数据；
- 保留确定性匹配和待确认状态；
- 不恢复通用推荐输出。

### 阶段 B：来源化商品属性

- 为试点品类建立属性注册表；
- 接入可信规格来源；
- 保存原始值、规范化值和 EvidenceRef；
- 实现单位换算、来源冲突和缺失值语义；
- 支持已知型号的规格比较。

### 阶段 C：受约束候选发现

- 将用户自然语言要求编译为品类属性约束；
- 开放目录召回，但身份不确定的候选不能自动进入正式结果；
- 建立硬约束过滤和推荐充分性门禁；
- 证据不足时只输出候选线索。

### 阶段 D：推荐与反馈闭环

- 定义可解释的品类排序策略；
- 收集曝光、点击、接受、排除和最终选择反馈；
- 建立冻结评测集和人工标注；
- 在证据覆盖和评测达标后恢复 `RECOMMENDATION` 产品语义；
- 只有真实标签和流量证明必要时，才考虑学习排序模型。

## 8. 质量指标与准入门槛

商品知识层不能只用“收录商品数量”衡量。建议至少跟踪：

- 商品身份匹配准确率；
- Variant 错误合并率；
- 同一 Variant 重复实体率；
- 配件、服务和替换件误入率；
- 关键属性证据覆盖率；
- 属性来源冲突率；
- 硬约束违反率；
- 推荐声明证据覆盖率；
- 缺失或冲突信息被正确保留为未知的比例；
- 过期知识和过期 Offer 的误发布率；
- 推荐稳定性与人工偏好一致率。

恢复通用推荐前，至少应满足：

1. 目标品类具有版本化的身份和属性规则；
2. 正式候选均解析到稳定 Product/Variant；
3. 每项参与过滤、排序和解释的属性都有来源；
4. 硬约束违反率为零或达到预先批准的严格门槛；
5. 身份不确定、属性不足和来源冲突都能失败关闭；
6. 推荐评测同时覆盖正确性、事实性、稳定性和 Provider 故障；
7. 产品不会把搜索覆盖率、库存记录或 Provider 排名冒充市场结论。

## 9. 对 RetailPriceAgent 当前路线的建议

当前报价线索产品不应为了未来可能的通用推荐而立即引入大规模知识基础设施。更合适的策略是：

1. 保留并继续验证当前报价闭环；
2. 把现有薄身份能力抽象为未来知识层的第一个纵向切片；
3. 优先增加 Product、Variant、Identifier 和属性证据模型；
4. 先支持“已知型号规格比较”，再逐步开放“按需求发现候选”；
5. 在商品身份、属性覆盖和离线评测成熟前，不恢复未经充分证明的推荐结论。

最终判断是：商品知识层是通用推荐助手从“搜索结果组织”走向“可信购买决策”的关键基础。当前收敛到精确型号报价，实质上暂时绕开了尚未完成的商品身份、属性和推荐知识问题；如果恢复通用推荐，应把建设商品知识层作为前置阶段，而不是在现有报价代码上直接增加一个 `RECOMMENDATION` 输出类型。
