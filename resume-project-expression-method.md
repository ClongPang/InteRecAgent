# 高水平项目简历表达方法

本文用于把复杂项目快速整理成面向技术招聘的高水平简历经历。它不提供可机械套用的文案，而是规定如何完成项目建模、信息选择、证据组织和最终表达。

## 一、核心认识

高水平简历表达首先不是遣词造句问题，而是从复杂项目中选择哪些事实，并把它们组织成怎样的证据链。

项目经历需要在有限篇幅内回答五个问题：

1. 解决的问题是否真实且重要；
2. 最关键的技术矛盾是什么；
3. 候选人做了哪些需要判断和取舍的设计；
4. 如何证明这些设计有效；
5. 这些能力为什么适合目标岗位。

因此，简历的最小单位不是功能或模块，而是一个能够被验证的技术主张。

```text
领域中的具体失败
→ 方案选择与职责边界
→ 关键实现机制
→ 对应评测
→ 可解释的结果
```

“使用混合检索”只是功能；说明单路检索为什么失败、怎样组合召回和重排、用什么数据验证以及指标如何变化，才构成技术主张。

## 二、项目经历的三层布局

### 1. 项目定位

项目概述只需要说明：

```text
服务谁 → 完成什么任务 → 输出什么可信结果
```

通用结构：

> 面向【用户或业务场景】，构建能够【完成复杂任务】的 Agent，支持【关键交互或执行行为】，输出【可验证的最终结果】。

项目概述不能从框架、数据库或内部类名开始。技术栈只提供关键词索引，后续 bullet 必须证明这些技术确实参与了关键流程。

### 2. 能力主张

每条 bullet 只证明一个高价值能力：

```text
具体失败 → 设计判断 → 实现机制 → 验证证据
```

它不能只是“负责某模块”，也不能在一条中罗列多个彼此独立的能力。

### 3. 整体叙事

所有 bullet 共同形成一条执行或能力演进主线：

```text
完成任务
→ 控制模型与工具
→ 保证状态和事实可信
→ 处理失败、重试和回归
→ 用任务级评测证明效果
```

招聘者最终应当形成完整判断：候选人不仅能搭建 Agent，还能控制、诊断、评测和迭代 Agent。

## 三、从项目到简历的方法流

### 第一步：确定唯一岗位命题

先回答：希望这个项目主要证明什么岗位能力？

| 主投方向 | 项目应重点证明 |
| --- | --- |
| Agent 应用开发 | 复杂任务完成、工具使用、上下文、事实可信与任务评测 |
| Agent Runtime | 执行协议、状态管理、恢复、隔离、幂等与故障评测 |
| LLM 应用平台 | 开发抽象、模型和工具接入、租户治理、运行稳定性与开发效率 |

一个项目只有一个主命题，其他能力只能作为差异点。试图同时证明三个方向，通常会退化成技术清单。

### 第二步：从失败而不是功能盘点项目

不要先列 PostgreSQL、Function Calling、Outbox、Grafana 等技术，而要列项目中真实出现过的失败及后果，例如：

- 模型臆造参数或调用未授权工具；
- 多轮修改后条件漂移；
- 已有证据时仍重复调用付费 Provider；
- 报价身份冲突却进入推荐；
- 单轮回复正确但完整任务失败。

失败模式天然带有业务意义，也更容易引出设计取舍。

### 第三步：建立主张卡

每个可能写入简历的内容，先补全以下信息：

```text
主张名称：
业务场景：
具体失败：
失败后果：
简单方案为什么不够：
我的设计选择：
模型负责什么：
确定性代码负责什么：
关键实现机制：
评测对象：
评测方式：
评测结果：
代码、测试和数据证据：
面试官可以追问什么：
```

### 第四步：筛选高价值主张

为每个主张按五个维度分别打 0～2 分：

| 维度 | 判断问题 |
| --- | --- |
| 岗位相关性 | 这是目标岗位真正关注的能力吗？ |
| 设计判断 | 是否体现取舍，而不只是调用框架？ |
| 项目特异性 | 删除项目名后还能直接套给其他项目吗？ |
| 证据强度 | 是否有测试、轨迹、指标或真实运行支撑？ |
| 追问价值 | 能否围绕它展开有深度的技术讨论？ |

- 8～10 分：正式简历主 bullet；
- 6～7 分：作为补充或与相近主张合并；
- 5 分以下：从简历删除，保留在项目文档。

### 第五步：按岗位主线排序

Agent 应用项目通常按以下主线组织：

```text
任务与 Agent loop
→ 跨轮上下文
→ 计划与工具授权
→ 事实和证据校验
→ 任务级评测
```

Agent Runtime 项目通常按以下主线组织：

```text
执行协议
→ 状态和并发控制
→ 幂等与外部副作用
→ 故障恢复和可观测
→ 故障注入评测
```

LLM 平台项目通常按以下主线组织：

```text
开发抽象
→ 模型和工具接入
→ 权限与租户治理
→ 运行稳定性
→ 使用规模与开发效率
```

最终只保留三至五条主张，不为展示技术广度破坏叙事主线。

## 四、单条 bullet 的写作方法

先写逻辑骨架，再压缩成自然语言：

```text
针对什么具体失败
→ 为什么简单方案不够
→ 做了什么关键设计
→ 模型与确定性代码如何分工
→ 用什么方式验证
→ 得到什么结果
```

参考结构：

> **约十字设计说明**：针对【具体业务失败及后果】，设计【Agent 机制】，由【模型职责】与【确定性代码职责】共同完成【处理过程】；基于【评测对象和方法】验证，【结果】。

“业务问题、Agent 方案、评测”是写作时的内在结构，不应机械地显示为三个标签。

标题应体现设计判断。例如“跨轮状态与指代”“计划校验与工具授权”“报价准入与事实校验”“可恢复执行与提交”“多轮任务轨迹评测”，优于“上下文模块”“数据库模块”“工具调用”等功能名称。

每条只保留：

- 一个具体问题；
- 一个核心设计；
- 两至三个关键机制；
- 一个直接对应的评测结论。

具体商品、某次调用次数和单次调试现象属于证据材料或面试案例，不应进入正式主张。

## 五、让评测与主张一一对应

| 技术主张 | 合适的评测 |
| --- | --- |
| 计划与工具调用 | 必需操作召回率、漏调用、冗余调用、未授权调用 |
| 跨轮状态 | 条件修改正确率、undo 成功率、状态恢复一致性 |
| 指代解析 | 正确绑定任务数/总任务数，并按修改和重排场景拆分 |
| 事实校验 | 必选条件违规率、无依据事实数、事实可追溯率 |
| 候选复用 | 零 Provider 任务数、冗余调用数、调用成本 |
| 持久化执行 | 过期提交数、状态重复提交数、Provider 重复调用数 |
| 完整 Agent 能力 | 任务级成功率、`pass@1`、`pass^k` |
| 工程效率 | 人工耗时、失败定位耗时、回归周期 |
| 运行效率 | tokens、单任务成本、p50/p95 延迟 |

不能用低层证据证明高层能力。证据强度通常依次为：

```text
单元测试
→ 集成测试
→ 确定性多轮轨迹
→ 真实 LLM 重复运行
→ 人工 Gold 评测
→ 真实用户或线上数据
```

单元测试不能证明真实模型成功率，单次零 Provider 调用不能证明平均成本下降，测试文件数量也不能证明用户任务完成质量。

## 六、数字的使用规则

高水平简历不是每条必须有数字，而是每个数字都必须能够解释。使用指标前至少要回答：

```text
评测多少个任务？
每个任务运行几次？
使用什么模型、代码和数据版本？
基线是什么？
由确定性脚本还是人工判分？
失败样本有哪些？
如何复现？
```

## 七、快速生产流程

一次简历项目重构可以按以下顺序执行：

1. **定位，10 分钟**：确定目标岗位和项目唯一命题；
2. **失败盘点，20 分钟**：列出十至十五个真实失败模式及后果；
3. **主张建模，30 分钟**：填写主张卡，补齐设计选择和证据；
4. **筛选排序，15 分钟**：评分并保留三至五条；
5. **组织主线，10 分钟**：按照执行链或能力演进排序；
6. **压缩表达，25 分钟**：每条保留一个问题、一个设计和一个评测；
7. **可信度审核，10 分钟**：逐句定位代码、测试、轨迹与指标口径；
8. **招聘者扫描，5 分钟**：只读项目描述和标题，检查十五秒内能否理解项目与候选人优势。

## 八、术语与抽象层级专项审查

简历表达需要区分四个抽象层级：

| 层级 | 内容 | 是否适合正文 |
| --- | --- | --- |
| 业务能力 | 多轮需求管理、工具编排、候选校验、证据约束、任务恢复 | 应作为主叙事 |
| 行业机制 | Agent Runtime、结构化计划、幂等、租约、事务提交、故障注入 | 可用于说明设计判断 |
| 项目内部对象 | `ConversationTurnDraftHost`、`Goal`、`TurnPlan`、`attempt`、`outbox` | 先翻译，必要时首次附内部名 |
| 测试样例 | 256GB、某个型号、成色、单次报价过滤数量 | 只进入证据文档或面试案例 |

正式简历以业务能力和行业机制为主。内部对象只有同时满足“行业可识别、能够体现设计、删除后会损失关键信息”时才保留；具体测试样例不能代替通用能力。

### 1. 内部命名不能冒充行业术语

- `Host` 在代码中可以是组件名，但脱离上下文没有统一含义；正文应写“Agent 运行时”或“规则校验与执行层”。
- `Goal` 应写成“结构化购物目标”或“结构化需求状态”；只有面试讲代码模型时再说明内部类型名。
- `TurnPlan` 首次可写成“结构化执行计划（TurnPlan）”，后续直接写执行计划。
- `attempt` 应写成“执行实例”或“重试任务”；`Provider` 应写成具体职责，如“外部报价服务”。
- `outbox`、`dead letter` 等词只有在主投 Runtime 岗位且确实体现关键取舍时保留，否则改写为“事务事件”和“失败事件处理”。

### 2. 标准术语也不一定适合直接写入简历

术语真实存在不代表具有简历价值。例如 `fencing token` 是分布式系统中的正式机制，但若正文重点是 Agent 应用能力，更适合写成“通过任务租约与执行版本校验阻止失效任务提交”。标准术语是否保留，需要连续回答：

1. 目标岗位招聘者能否快速理解；
2. 它是否表达可迁移的设计能力，而非实现细枝末节；
3. 删除它是否会损失关键技术判断；
4. 候选人能否用一句话解释其必要性与实现边界。

任一答案为否时，优先翻译为职责和效果。

### 3. 具体字段和品类样例需要上提

“校验预算、市场、型号、成色”描述的是若干测试字段，不是规划校验能力。应上提为：

> 结合用户原文、会话状态与工具契约，校验计划结构、约束来源、候选引用和调用权限。

“过滤配件、型号冲突和价格来源缺失”同样偏向数码品类，应上提为：

> 识别候选身份冲突、硬性约束不满足和来源证据缺失，执行候选准入与结果降级。

具体字段仍需保留在评测集，用于证明抽象能力，但不占据简历第一层表达。

### 4. 写作结构不能变成显式答题模板

“业务问题 → Agent 方案 → 评测”是构思和审核时的逻辑骨架，不是正文标签。最终 bullet 应自然形成因果链：从失败场景起句，以设计动作承接，以验证结果收束。显式写出“业务问题：”“Agent 方案：”“评测：”会破坏叙事连续性，并暴露模板痕迹。

### 5. 项目定位先于架构细节

项目描述优先回答：

```text
应用领域
→ 复杂任务
→ 系统范式
→ 核心能力或可信产物
```

它不应同时塞入内部组件、完整执行链和价值口号。招聘者应先理解“这是一个什么 Agent、解决什么任务”，再通过 bullet 理解内部设计。

### 6. 评测名称需要面向读者统一

- 避免在同一句混用 `Gold Set`、`Replay`、`trial`、`task` 与中文计数单位；
- 首次可使用“黄金测试集（Gold Set）”“固定数据回放”，后续统一使用中文；
- “协议违规逃逸率”“预登记终态与副作用断言”等内部测试语言，应分别改写为“越权调用拦截结果”“故障注入通过率”等可直接理解的指标；
- 指标的精确定义、分子和分母放在证据文档，正文只保留能够解释技术主张的名称与结果。

### 7. 专项检查清单

每次改稿后单独执行以下检查：

- 是否出现只有项目作者才能解释的首字母缩写、类名或字段名；
- 是否把某个品类、某次联调或某条报价写成通用能力；
- 是否把标准但低价值的底层术语堆进 Agent 应用简历；
- 是否存在中英文混用但没有增加信息的表达；
- 是否重复出现项目描述已经交代过的架构分工；
- 是否能将每个名词对应到业务失败、设计选择和验证结果；
- 是否能让不了解代码仓库的面试官在十五秒内复述该条能力。

## 九、最终质量门禁

一条 bullet 进入正式简历前，应全部满足：

- 描述失败和解决方案，而不是岗位职责；
- 只能描述当前项目，不能无差别套用；
- 包含个人设计判断和必要取舍；
- 写清模型与确定性代码的职责边界；
- 评测方式与技术主张直接对应；
- 所有数字可解释、可复现；
- 不用单个案例冒充总体结果；
- 不把内部类名包装成行业概念；
- 能够自然口述；
- 能承受面试官连续追问。

最终方法流可以概括为：

```text
定位岗位
→ 盘点真实失败
→ 提炼技术主张
→ 绑定实现证据
→ 按执行链排序
→ 用自然因果句表达
→ 用对应评测证明
→ 完成可信度与面试审核
```

目标不是复制优秀简历的术语、句式或数字，而是把自己的项目重新组织成一组岗位相关、体现判断并且能够验证的技术主张。
