# RetailPriceAgent 简历项目经历

**RetailPriceAgent｜商品报价查询 Agent**　个人项目 / Agent 开发　2026.07–至今

**项目描述**：面向跨市场购物中的多轮需求澄清、报价检索与候选比较，构建具备需求状态跟踪、受控工具调用、候选规则校验、来源追踪与任务恢复能力的对话 Agent；对校验策略完整的品类输出正式推荐，其余品类仅提供带能力边界说明的候选线索。

**技术栈/数据源**：TypeScript、pi-agent-core、Fastify、PostgreSQL、DeepSeek、BuyWhere API、Langfuse、OpenTelemetry

- **多轮状态与候选指代**：针对需求与候选集合持续演进导致的状态偏移和指代失稳，以 PostgreSQL Revision 持久化购物目标与候选集，每轮从有界快照恢复上下文，并在执行前将自然语言指代绑定为稳定候选标识；11 组离线多轮用例覆盖状态演进、候选操作与版本回退，已有来源记录可用时保持零外部报价搜索，撤销后精确恢复目标 Revision。

- **计划校验与工具授权**：针对复合请求中的漏操作、无依据参数和越权调用，设计 `plan commit → controlled execution → structured response` 协议；LLM 提交结构化操作计划，确定性执行层结合用户原文、会话状态和工具契约完成参数来源检查、计划冲突消解与外调授权；构建 6 类 30 项协议负向测试，30/30 进入预期拒绝或受控降级，未发布越权回复。

- **候选规则校验与来源追踪**：针对异构报价中候选语义、用户约束与来源字段无法直接对齐的问题，将 Provider 原始响应规范化为带原始 JSON Path、观察时间和派生关系的可追踪字段，按品类校验策略检查型号、主商品/配件、市场与硬约束；正式回复只能引用与当前候选及来源记录一致的声明，校验能力不足时降级为带边界说明的候选线索。

- **可恢复话轮处理与故障恢复**：针对 Worker 接管和进程中断引发的旧任务覆盖、工具重复执行与回复重复提交，以数据库 Lease、Fencing Token 和 Revision 校验隔离失效执行，按幂等步骤账本复用成功结果，并在 PostgreSQL 事务内原子提交状态、回复与事件；PostgreSQL/API 集成门禁 23/23 通过，覆盖过期写入拒绝、幂等重放、事务回滚及 Outbox 失败隔离。

- **多轮任务评测与可观测性**：针对单轮回复正确但完整购物任务失败、模型异常难定位的问题，构建覆盖 13 类 39 项任务的预登记评测设计，计划在独立数据集上完成 117 次真实模型运行；冻结 Prompt、模型、回放数据和评分器版本，并将模型推理、工具调用、执行步骤与最终状态关联至同一 Trace；已完成任务级评分器、Replay Provider、离线指标回归门禁和 Langfuse 实验链路，正式独立评测尚未完成，不提前报告成功率。
